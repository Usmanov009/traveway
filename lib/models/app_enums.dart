enum TabType { search, explore, hot, trips, profile }

enum SearchMode { packages, itinerary }

enum Language { uz, ru, en }

enum AppThemeMode { dark, light }

enum BadgeType { ultra, luxury, cheap, culture }

enum CardType { humo, uzcard, visa }

enum BookingType { active, completed, cancelled }
